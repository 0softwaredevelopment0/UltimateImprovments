name: UI-Chat
version: ${project.version}
description: UltimateImprovments — Chat system (channels, pings, filter)
main: com.ultimateimprovments.chat.UIChat
api-version: '26.2'
author: rizer001
addon-for: UI-Core
depend: [UI-Core, UI-Punish]
softdepend: [PlaceholderAPI, LuckPerms, Vault]
